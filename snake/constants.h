const int screenWidth = 640;
const int screenHeight = 480;

 const float blocX = 20.0f;
const float blocY = 20.0f;

const float thikness = 3.0f;

 const float blockSize = blocX - 2 * thikness;

const int mapBlocCountX = 30;
const int mapBlocCountY = 20;

const float gameRectX = 20.0f;
const float gameRectY = 20.0f;

const float gameRectWidth = blocX * mapBlocCountX;
const float gameRectHeight = blocY * mapBlocCountY;

